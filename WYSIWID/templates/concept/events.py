# Event definitions emitted by this concept

class Events:
    CREATED = "concept.created"
    UPDATED = "concept.updated"
    DELETED = "concept.deleted"

# def emit_item_created(item):
#     event_bus.emit(Events.CREATED, item)
